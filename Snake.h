#pragma once

#include "SDL2/SDL.h"


/*void init_snake_body(Body *, Body *);
void create_snake(SDL_Renderer *, Snake *);
void reset_body(Body *);
Body * get_snake_tail(Body *, int);
Body *get_next_body(Body *);
SDL_Rect *get_body_at(Snake *, int);*/

/**
 * Traverses to the end of the tail and makes it longer
 */
//void snake_nibble(Snake *);
